# chunk-LHKS7QUN.js

## Overview

The **Angular framework core chunk**. This is the largest and most fundamental Angular runtime bundle, containing the engine that powers reactivity, dependency injection, rendering, and change detection across the entire application.

## Dependencies

| Import | Purpose |
|--------|---------|
| `chunk-TWZW5B45.js` | Low-level object utilities (`V`, `G`, `h`) |

## Key Systems Included

### Reactive Signals

Angular's signal-based reactivity system (introduced in Angular 16+):

| Symbol | Role |
|--------|------|
| `$t` (Signal) | Reactive value container |
| `qo` / `M` | Get / set the active consumer context |
| `Ge` / `Uo` | Get / set the current reactive node |
| `Nn` | Check if a value is the `NotFound` sentinel |

The signal graph tracks producer→consumer relationships. When a signal's value changes, all dependent consumers are marked dirty and scheduled for re-execution.

### Dependency Injection (DI)

- **`wa`** — current injector context
- **`Ef`** — `Symbol("NotFound")` sentinel returned when a token has no provider
- Provider lookup traverses the injector tree from child to root, returning `NotFound` if no match is found

### Change Detection

Zone.js integration hooks that schedule Angular's change detection cycle after async operations complete.

### Renderer / DOM Abstraction

Low-level DOM manipulation utilities that Angular components use instead of touching `document` directly, enabling server-side rendering (SSR) compatibility.

## Exports

This chunk exports hundreds of symbols consumed by `chunk-4MIYPPGW.js` and `main.js`. They include Angular decorators (`@Component`, `@Injectable`, `@NgModule`), lifecycle hooks, built-in pipes, directives, and the router.

## Notes

- Do not edit this file directly; it is generated output from the Angular compiler.
- The content hash in `main.js`'s imports ties this chunk to a specific build.
