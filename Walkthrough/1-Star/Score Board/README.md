# Score Board

* Type: `Miscellaneous`

## Part 1
### Tip
Find the carefully hidden 'Score Board' page.

### Solution
Just access the url: `http://127.0.0.1/#/score-board`

## Part 2 - `With Coding Challenge`

### Find It
we prompted an `node-js` code with a content of the `const routes: Routes`, we should fine the route ralevant

to the `score-board`, so it there on `114` and `115`, just mark them and `submit`.

### Fix It
We can't fix this without hurting the "normal-usage and activity" of the website... It's a feature, not a bug...

Pick the `fix` option that has a comment saying it "Must remain as is!"

